# Quarto
/.quarto/
_book
_freeze/**/figure-*
**/*.quarto_ipynb

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Descargas y material auxiliar
*.zip

# Material de formación del que salen los cuadernos de notebooks/.
# Son .ipynb con salidas guardadas, pesan megabytes y no se publican.
_reference_notebooks/

# El almacén de la secretaría se genera con data/secretaria/generar.py a
# partir de una semilla fija. Versionarlo solo añadiría 4 MB de binario que
# cambia entero con cada reescritura. El corpus, que se edita a mano, sí va.
data/secretaria/*.duckdb
data/secretaria/*.duckdb.wal

# Índices que los cuadernos construyen al ejecutarse, junto al propio cuaderno.
notebooks/**/lancedb/

# Secretos
.env
